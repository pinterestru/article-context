# ⚠️ IMPORTANT: Never commit real API keys or sensitive data
# This file shows the required environment variables
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and will not be committed

# ============================================
# REQUIRED VARIABLES
# ============================================

# API Configuration
API_BASE_URL=<YOUR_API_BASE_URL>  # Required: Single API gateway URL for all services

# ============================================
# OPTIONAL SERVER-SIDE VARIABLES
# ============================================

# Media Configuration
# MEDIA_BASE_URL=https://api.example.com  # Custom base URL for media assets (defaults to API_BASE_URL)

# Ecommerce Store IDs
# ARTICLE_ECOMMERCE_STORE_ID=<YOUR_ARTICLE_ECOMMERCE_STORE_ID>
# PROMOCODE_ECOMMERCE_STORE_ID=<YOUR_PROMOCODE_ECOMMERCE_STORE_ID>

# Cache Configuration (defaults shown)
# PROMOCODE_CACHE_TTL=3600  # Promocode cache TTL in seconds (default: 3600 = 1 hour)
# ARTICLE_CACHE_TTL=3600    # Article cache TTL in seconds (default: 3600 = 1 hour)

# Environment
NODE_ENV=development  # Options: development, production, test (default: development)

# Site Configuration
# SITE_CONFIG=default  # Options: default, veil, or custom site config name (default: default)

# Locale Configuration
# NEXT_PUBLIC_DEFAULT_LOCALE=en  # Options: en, ru (default: en) - Used for client-side error boundaries

# Logging
# LOG_LEVEL=info  # Options: trace, debug, info, warn, error, fatal (default: info)

# Feature Flags
# ENABLE_OPENTELEMETRY=false  # Enable OpenTelemetry tracing (default: false)
# CLOAK_DISABLED=false  # Disable cloaking/localization - shows all content when true (default: false)

# ============================================
# CLIENT-SIDE VARIABLES (NEXT_PUBLIC_*)
# ============================================

# Website Configuration
NEXT_PUBLIC_WEBSITE_DOMAIN=yourdomain.com  # Your website domain (without https://)

# Analytics (Optional - remove if not using)
# NEXT_PUBLIC_GA4_MEASUREMENT_ID=<YOUR_GA4_MEASUREMENT_ID>
# NEXT_PUBLIC_GTM_ID=<YOUR_GTM_ID>
# NEXT_PUBLIC_YANDEX_METRICA_ID=<YOUR_YANDEX_METRICA_ID>

# Monitoring (Optional - remove if not using)
# NEXT_PUBLIC_SENTRY_DSN=<YOUR_SENTRY_DSN>  # Used for both client and server-side error tracking

# ============================================
# OPENTELEMETRY CONFIGURATION (Optional)
# ============================================

# These are standard OpenTelemetry env vars, not defined in our config but used by the SDK
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318  # OTLP collector endpoint
# OTEL_EXPORTER_OTLP_HEADERS={"Authorization": "Bearer <YOUR_TOKEN>"}  # Optional headers as JSON